[English](README.md) | [简体中文](README_zh-CN.md) | [繁體中文](README_zh-TW.md) | [Deutsch](README_de-DE.md) | [Français](README_fr-FR.md) | [Русский](README_ru-RU.md) | [Português](README_pt-BR.md) | [日本語](README_ja-JP.md) | [Español](README_es-ES.md) | [한국어](README_ko-KR.md) | [Tiếng Việt](README_vi-VN.md)

# DocWen Assistant - Плагин Obsidian

Плагин Obsidian для настольного приложения [DocWen](https://github.com/ZHYX91/docwen).

## ✨ Функции

### Основные функции
- ✅ **Быстрый запуск из боковой панели**: Добавьте иконку на боковую панель Obsidian для запуска одним кликом
- ✅ **Автоматическая передача файлов**: Автоматически передаёт путь к текущему открытому файлу в DocWen
- ✅ **Интеграция с палитрой команд**: Быстрый доступ через Ctrl/Cmd + P
- ✅ **Фоновый экспорт (CLI)**: Экспорт в Word/Excel/Markdown через DocWenCLI.exe без открытия GUI (при необходимости появится выбор)
- ✅ **Нумерация заголовков (CLI)**: Добавление/удаление нумерации заголовков Markdown через DocWenCLI.exe
- ✅ **Проверка doctor (CLI)**: Проверка окружения/диагностика в один клик
- ✅ **Проверка пути**: Проверка пути к исполняемому файлу в реальном времени
- ✅ **Браузер файлов**: Легко выберите исполняемый файл через диалог просмотра
- ✅ **Обратная связь об успехе**: Дружественные уведомления при запуске
- ✅ **Контекстное меню (правый клик)**: Правый клик по файлу в проводнике → подменю DocWen (конвертация форматов, нумерация заголовков, открыть в DocWen)
- ✅ **Управление единственным экземпляром**: Автоматически отправляет файл в работающий экземпляр
- ✅ **Многоязычная поддержка**: Поддержка 11 языков (zh-CN, zh-TW, en, de, fr, ru, pt-BR, ja, ko, es, vi)

---

## 📦 Быстрый старт

### Предварительные требования

1. **Установите Node.js**
   - Посетите [официальный сайт Node.js](https://nodejs.org/)
   - Скачайте и установите версию LTS
   - Проверьте установку: `node -v` и `npm -v`

2. **Установите зависимости**
   ```bash
   npm install
   ```

### Режим разработки

Используйте режим watch во время разработки для автоматической перекомпиляции при изменении кода:

```bash
npm run dev
```

### Сборка плагина

#### Быстрая сборка (без проверки типов)
```bash
npm run build:quick
```

#### Полная сборка (с проверкой типов и минификацией)
```bash
npm run build
```

#### Сборка релиза (автоматическая упаковка)
```bash
npm run release
# Или запустите напрямую: node scripts/build.js
```

Эта команда:
1. Скомпилирует код TypeScript
2. Создаст директорию release
3. Скопирует необходимые файлы
4. Сгенерирует инструкции по использованию

---

## 🚀 Установка в Obsidian

### Способ 1: Скачать релиз (Рекомендуется)

1. Перейдите на страницу [GitHub Releases](https://github.com/ZHYX91/docwen-obsidian/releases)
2. Скачайте и распакуйте последнюю версию
3. Скопируйте папку `docwen-assistant` в `<Ваш Vault>/.obsidian/plugins/`
4. В Obsidian: `Настройки` → `Сторонние плагины` → `Перезагрузить плагины` → Включить `DocWen Assistant`

### Способ 2: Сборка из исходного кода

1. Установить зависимости и собрать:
   ```bash
   npm install
   npm run release
   ```
2. Скопировать папку `release/docwen-assistant` в `<Ваш Vault>/.obsidian/plugins/`
3. Перезагрузить и включить плагин в Obsidian

---

## ⚙️ Настройка

1. Откройте Obsidian `Настройки` → `Сторонние плагины` → `DocWen Assistant`

2. Укажите путь к GUI или CLI (достаточно одного):
   - Полный путь к `DocWen.exe` или `DocWenCLI.exe`
   - Если указан только один, второй будет автоматически обнаружен в той же папке

3. Проверка пути:
   - ✓ Зелёный означает допустимый путь
   - ✗ Красный означает недопустимый путь или файл не найден

---

## 📖 Использование

### Запуск DocWen

Три способа запуска:

1. **Иконка боковой панели**
   - Нажмите на иконку документа в левой боковой панели

2. **Палитра команд**
   - Нажмите `Ctrl/Cmd + P` для открытия палитры команд
   - Найдите "Запустить DocWen"

3. **Запуск с текущим файлом**
   - Найдите "Запустить DocWen с текущим файлом" в палитре команд
   - Доступно только когда файл открыт

### Фоновый экспорт (CLI, без открытия GUI)

Найдите в палитре команд:
- «Экспорт в Word (Docx) в фоне» — для файлов `.md`/`.markdown`/`.txt` выберите шаблон
- «Экспорт в Excel (XLSX) в фоне» — для файлов `.md`/`.markdown`/`.txt` выберите шаблон
- «Экспорт в Markdown (MD) в фоне» — если доступны типы оптимизации для типа файла и языка, выберите один (или пропустите)

Требуется `DocWenCLI.exe`.

### Контекстное меню (правый клик)

Щёлкните правой кнопкой мыши по файлу в проводнике, чтобы увидеть подменю **DocWen**. Доступные действия зависят от типа файла:

- **Конвертировать в Markdown** — для файлов docx, xlsx, pdf, изображений и др.
- **Конвертировать в Word (Docx)** / **Конвертировать в Excel (XLSX)** — для файлов `.md`/`.markdown`/`.txt`
- **Добавить/Удалить нумерацию заголовков** — только для файлов `.md`
- **Открыть в DocWen** — доступно для всех файлов

### Нумерация заголовков (CLI)

Найдите в палитре команд:
- «Добавить нумерацию к заголовкам Markdown» — выберите схему нумерации
- «Удалить нумерацию из заголовков Markdown»

Доступно только когда открыт файл `.md`. Требуется `DocWenCLI.exe`.

### Проверка doctor (CLI)

Найдите в палитре команд:
- «Проверка doctor DocWen»

Требуется `DocWenCLI.exe`.

### Автоматическая передача файлов

- Если файл открыт, плагин автоматически передаёт его полный путь в DocWen
- Если файл не открыт, запускается только программа DocWen

### Управление единственным экземпляром

- **Первый клик** → Запуск DocWen и передача текущего файла
- **Повторный клик (с файлом)** → Замена на новый файл (Режим одного файла)
- **Повторный клик (без файла)** → Активация окна DocWen

---

## 🛠️ Скрипты разработки

### Доступные команды

| Команда | Описание |
|---------|----------|
| `npm run dev` | Режим разработки (watch) |
| `npm run build` | Полная сборка (проверка типов + минификация) |
| `npm run build:quick` | Быстрая сборка (без проверки типов) |
| `npm run lint` | Проверка ESLint |
| `npm run lint:fix` | Автоисправление ESLint |
| `node version-bump.js [patch\|minor\|major]` | Обновить номер версии |
| `npm run release` | Собрать пакет релиза |

### Управление версиями

Обновление номера версии:

```bash
# Патч-версия (1.0.0 → 1.0.1)
node version-bump.js patch

# Минорная версия (1.0.0 → 1.1.0)
node version-bump.js minor

# Мажорная версия (1.0.0 → 2.0.0)
node version-bump.js major
```

---

## 📁 Структура проекта

```
docwen-obsidian/
├── src/                 # 📁 Директория исходного кода
│   ├── main.ts          # Основная логика плагина
│   ├── settings.ts      # Страница настроек
│   ├── i18n.ts          # Модуль интернационализации
│   └── utils/           # Вспомогательные модули
│       └── suggest-modal.ts # Модальное окно выбора
├── dist/                # 🔨 Директория вывода сборки
│   └── main.js          # Скомпилированный код
├── docs/                # 📄 Документация
│   └── plugin-readme/    # README для пользователей (многоязычный)
├── scripts/             # 📜 Скрипты сборки
│   ├── build.bat        # Сборка одним кликом для Windows
│   ├── build.js         # Кроссплатформенный скрипт сборки
│   └── README.md        # Руководство по использованию скриптов
├── release/             # 📦 Артефакты релиза
├── .vscode/             # 🛠️ Конфигурация редактора
│   └── settings.json    # Настройки VS Code
├── manifest.json        # Манифест плагина
├── package.json         # Конфигурация проекта
├── tsconfig.json        # Конфигурация TypeScript
├── eslint.config.cjs    # Конфигурация ESLint
├── .gitignore          # Файл Git ignore
├── version-bump.js     # Скрипт управления версиями
├── README.md           # Документация на английском
└── README_ru-RU.md     # Этот документ (Русский)
```

---

## 🐛 Устранение неполадок

### Плагин не загружается

1. Проверьте, правильно ли скопированы `main.js` и `manifest.json`
2. Нажмите `Перезагрузить плагины` в Obsidian
3. Проверьте консоль разработчика (`Ctrl/Cmd + Shift + I`) на наличие ошибок

### Не удаётся запустить DocWen

1. Проверьте правильность пути к исполняемому файлу
2. Подтвердите, что статус пути показывает зелёный ✓
3. Подтвердите, что исполняемый файл имеет правильные разрешения

### Путь к файлу не передаётся

1. Подтвердите, что файл в данный момент открыт
2. Проверьте, содержит ли путь к файлу специальные символы
3. Проверьте логи консоли на переданные аргументы

---

## 📜 Лицензия

Этот проект лицензирован под лицензией MIT.

### Контакты

- **GitHub**: https://github.com/ZHYX91/docwen-obsidian
- **Основной проект DocWen**: https://github.com/ZHYX91/docwen
- **Связаться с автором**: zhengyx91@hotmail.com

---

**Автор**: ZhengYX
